{
    "name": "copy-file-name",
    "displayName": "copy-file-name",
    "description": "Copy the name of the currently opened file to the clipboard.",
    "version": "0.0.1",
    "publisher": "nemesv",
    "engines": {
        "vscode": "^1.0.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:copy-file-name.copyFileName",
        "onCommand:copy-file-name.copyFileNameWithExtension",
        "onCommand:copy-file-name.copyFullPath"
    ],
    "main": "./out/src/extension",
    "contributes": {
      "keybindings": [
        {
          "command": "copy-file-name.copyFileName",
          "key": "ctrl+alt+f",
          "mac": "cmd+alt+f",
          "when": "editorTextFocus"
        },
        {
          "command": "copy-file-name.copyFileNameWithExtension",
          "key": "ctrl+alt+e",
          "mac": "cmd+alt+e",
          "when": "editorTextFocus"
        },
        {
          "command": "copy-file-name.copyFullPath",
          "key": "ctrl+alt+p",
          "mac": "cmd+alt+p",
          "when": "editorTextFocus"
        }
      ],
      "commands": [
        {
          "command": "copy-file-name.copyFileName",
          "title": "Copy file name to clipboard"
        },
        {
          "command": "copy-file-name.copyFileNameWithExtension",
          "title": "Copy file name with extensions to clipboard"
        },
        {
          "command": "copy-file-name.copyFullPath",
          "title": "Copy full path to clipboard"
        }
      ]
    },
    "scripts": {
        "vscode:prepublish": "node ./node_modules/vscode/bin/compile",
        "compile": "node ./node_modules/vscode/bin/compile -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install"
    },
    "devDependencies": {
        "typescript": "^1.8.5",
        "vscode": "^0.11.0"
    },
    "dependencies": {
        "copy-paste": "^1.2.0"
    }
}